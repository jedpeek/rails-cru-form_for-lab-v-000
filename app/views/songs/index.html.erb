<h1> Songs Page </h1>
<% @songs.each do |song| %>
<% artist = Artist.find(song.artist_id) %>
<% genre = Genre.find(song.genre_id) %>
<h1> <%= link_to(song.name, song_url(song)) %></h1>
<h4> <%= link_to(song.genre_id, genre_url(song.genre_id)) %></h4>
<h4> <%= link_to(song.artist_id, artist_url(song.artist_id)) %></h4>
<% end %>
